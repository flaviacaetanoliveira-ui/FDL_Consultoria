.streamlit/secrets.toml
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.env
*.xlsx~
~$*

# Dados reais em cliente_1 (estrutura versionada; ficheiros não)
cliente_1/**/*.xlsx
cliente_1/**/*.xls
cliente_1/**/*.csv
cliente_1/**/*.zip
!cliente_1/**/.gitkeep

# Agendamento local (caminhos da maquina)
agendamento/config_local.bat
agendamento/logs/

# Snapshot de materialização versionado para Streamlit Cloud (repasse/frete em data_products/default/.../current/).
# Regenerar localmente com: python run_materialize_financeiro.py --base-dir ... --modulo all
# Outros segmentos de cliente local (não versionar):
data_products/fdl_cli/
# Paridade local opcional: processing/validation/reports/
processing/validation/reports/

# Logs do runner run_materialize_financeiro.py (máquina local / agendador)
logs/

# Logs locais do mirror Power BI / agendamento
powerbi_mirror/output/*.log
